/*NSMutableArray *combinedArray = [[NSMutableArray alloc]init];
                    NSArray *oiuda = (NSArray *)objInUdDict;
                    NSArray *oida = (NSArray *)objInDownloaded;
                    
                    int count = 0;
                    BOOL oidaIsBigger = NO;
                    
                    if (oida.count >= oiuda.count) {
                        count = oiuda.count;
                        oidaIsBigger = YES;
                    } else {
                        count = oida.count;
                        oidaIsBigger = NO;
                    }
                    
                    
                    // int count = (oida.count >= oiuda.count) ? oiuda.count : oida.count; // ? YES : NO;
                    
                    for (int i = 0; i < count; i++) {
                        id one = [oiuda objectAtIndex:i];
                        id two = [oida objectAtIndex:i];
                        
                        [combinedArray addObject:one];
                        
                        if (one != two) {
                            [combinedArray addObject:two];
                        }
                    }
                    
                    if (combinedArray.count < count) { // count is smaller number

                        if (oidaIsBigger) {
                            for (int z = combinedArray.count; z < oida.count; z++) {
                                [combinedArray addObject:[oida objectAtIndex:z]];
                            }
                        } else {
                            for (int z = combinedArray.count; z < oiuda.count; z++) {
                                [combinedArray addObject:[oiuda objectAtIndex:z]];
                            }
                        }
                    } else { // count is bigger or equal, do nothing
                        
                    }*/